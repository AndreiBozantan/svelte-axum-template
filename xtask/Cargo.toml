[package]
name = "xtask"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true


[[bin]]
name = "xtask"
path = "xtask.rs"

[dependencies]
# No external dependencies are strictly required. Keeping dependencies empty ensures near-instant build/execution time.
